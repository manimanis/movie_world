import { TextTruncPipe } from './text-trunc.pipe';

describe('TextTruncPipe', () => {
  it('create an instance', () => {
    const pipe = new TextTruncPipe();
    expect(pipe).toBeTruthy();
  });
});
